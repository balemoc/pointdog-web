body
  display flex
  flex-direction column
  justify-content center
  align-items center

app
  /* until content loaded */ min-width: 60%;
  background-color: #3c3c3c
  display flex
  flex-direction column
  justify-content center
  max-width 70%

html
  background url("/assets/img/pointdog_background.png") no-repeat center center fixed
  -webkit-background-size cover
  -moz-background-size cover
  -o-background-size cover
  background-size cover
