import template from './pincode.html';
import controller from './pincode.controller';
import './pincode.styl';

const pincodeComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller,
};

export default pincodeComponent;
